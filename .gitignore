.DS_Store
**/.DS_Store

# El código fuente completo del template Intrio (pagado, licencia de
# Designesia/ThemeForest) se mantiene solo local. No se redistribuye
# aquí — este repo documenta el proyecto y guarda nuestros propios
# archivos (overrides de tema, imágenes de referencia).
Intrio HTML/
